#!/usr/bin/env bash
# asdf list-all: fetch all versions from GitHub Releases
set -euo pipefail

REPO="anomalyco/safeselect"

git ls-remote --tags "https://github.com/${REPO}.git" \
  | grep -oP 'refs/tags/v?\K[0-9]+\.[0-9]+\.[0-9]+' \
  | sort -V \
  | uniq
